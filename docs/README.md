# docs

`governance-loop.png` — a single-frame screenshot of the AI Governance page showing the full
loop: a prompt, the safe-command preview, an approved draft in the review queue, and the audit
log. Capture it from `/governance` after running one demo prompt through approval.

This image is referenced from the top-level `README.md` and is intended as a standalone visual
(no need to clone or run the app to understand the governance loop).
